version = "0.0.1"
description = "Recruitment task for Whitebox"

subprojects {
    apply plugin: 'java'
    apply plugin: "groovy"
    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(17)
        }
    }
}
